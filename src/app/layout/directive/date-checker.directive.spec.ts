import { DateCheckerDirective } from './date-checker.directive';

describe('DateCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new DateCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
